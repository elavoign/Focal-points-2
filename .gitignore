# OS / IDE artifacts
.DS_Store
**/.DS_Store
.Rhistory
.RData
.Rproj.user/

# targets computed objects (large, reproducible → do NOT track)
_targets/

# Processed and output data (large binaries → do NOT track)
data/
outputs/

# targets factory scripts are SOURCE CODE → DO track
# (only _targets/ computed objects are excluded above)

# Claude Code internal files
.claude/worktrees/
.claude/projects/
